# terraform/

This directory will contain infrastructure as code for Proxmox VE. It uses
the bpg provider, with one Terraform root per server. A later phase will
generate the inventory and create LXC containers from Packer images. The
directory stays a placeholder until that phase.
