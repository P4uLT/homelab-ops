# packer/

Golden LXC images for Proxmox VE, built with Packer and pinned by digest
(`images.lock.yml`). Cattle containers come from these images and are never
patched in place. Populated in a later phase.
