---
# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

# Delegator namespace for the Ansible shell (lands in phase 0b). Phase 0b
# appends the real delegator tasks (verify, lint, run) here. Until then the
# namespace stays empty so `task --list` works from the repo root.
tasks: {}
