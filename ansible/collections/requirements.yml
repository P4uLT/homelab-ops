---
# Vendor collections — exact pins only, installed to collections/vendors/ (gitignored).
# Entry: task -d ansible galaxy
collections:
  - name: community.sops
    version: 2.4.0
  # Only for the ansible.posix.profile_tasks timing callback.
  - name: ansible.posix
    version: 2.2.2
