---
# Static inventory — pets by nature. Structure and hostnames only:
# network locators and credentials live in SOPS-encrypted group_vars.
all:
  children:
    grp_servers:
      children:
        grp_servers_proxmox:
          children:
            grp_servers_proxmox_jarvis:
              hosts:
                jarvis:
