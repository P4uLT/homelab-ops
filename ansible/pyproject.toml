[project]
name = "homelab-ops-ansible"
version = "0.0.0"
description = "Ansible runtime configuration for the homelab-ops monorepo."
requires-python = ">=3.14,<3.15"
dependencies = [
    "ansible-core==2.21.4",
    "ansible-lint==26.8.0",
    "jmespath==1.1.0",
    "netaddr==1.3.0",
]

[tool.uv]
package = false
