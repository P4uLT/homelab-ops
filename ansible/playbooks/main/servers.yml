---
# Orchestrator for the server audience — fixes the spoke order (common,
# then monitoring, services, stacks, bootstrap). Empty groups mean skipped
# plays, so one order fits every host. Populated during the content-import
# phase.
