---
# Vendor roles — exact pins only, installed to roles/vendors/ (gitignored).
# Entry: task -d ansible galaxy
roles:
  - name: lae.proxmox
    src: git+https://github.com/lae/ansible-role-proxmox.git
    version: v1.10.1
